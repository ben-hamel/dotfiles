# Dotfiles

Managed with [GNU Stow](https://www.gnu.org/software/stow/). Each top-level directory is a Stow package whose structure mirrors `$HOME`.

To add a new config, use the `add-dotfiles-config` skill.
